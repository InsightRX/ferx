[package]
name = "ferx"
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["InsightRX, Inc."]

[lib]
crate-type = ["staticlib"]

[dependencies]
extendr-api = "0.7"
ferx-nlme = { path = "../../../ferx-nlme", features = ["autodiff"] }
nalgebra = "0.33"

[profile.release]
lto = "fat"
